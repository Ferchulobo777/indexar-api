# Copiar a .env (o exportar como variables de entorno) para desarrollo local.
# En producción estas variables las setea la plataforma de deploy (Render/Railway/Fly.io),
# nunca se commitea un .env real.

DB_URL=jdbc:postgresql://localhost:5432/indexar
DB_USER=indexar
DB_PASSWORD=indexar
PORT=8080
